dockercontainers:
  hosts:
    c01:
      ansible_host: localhost
      ansible_port: 2222
    c02:
      ansible_host: localhost
      ansible_port: 2223
  # SSH key variables, that are used by all hosts
  vars:
    ansible_user: iida
    ansible_ssh_private_key_file: ~/.ssh/iida_id_rsa
